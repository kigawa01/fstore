ktor:
    application:
        modules:
            - net.kigawa.fstore.ApplicationKt.module
    deployment:
        port: 8080
jwt:
  domain: "https://jwt-provider-domain/"
  audience: "jwt-audience"
  realm: "ktor sample app"
db:
  mongo:
    user: "$DB_USER:mongo"
    password: "$DB_PASS:mongo"